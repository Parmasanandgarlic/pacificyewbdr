name: Pacific Yew BDR Legacy - Superseded

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow identity is retired."
          echo "Production delivery is owned by .github/workflows/bdr-live.yml."
          echo "Manual delivery remains disabled."
